
// agendar trabajo
// form agendar
$(document).ready(function() {
    $('#formagendar').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// detalles generales
// espera modificar
$(document).ready(function() {
    $('#formdetallesModificarEspera').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});
// detalles generales
// realizados modificar
$(document).ready(function() {
    $('#formaModRealizado').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// equipamientos form accesorios
// registrar nuevo accesorios
$(document).ready(function() {
    $('#formaccesorios').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});
// equipamientos form equipamiento
// registrar nuevo equipamientos
$(document).ready(function() {
    $('#forregistrarEquipamiento').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});


// inspecciones en epsera modificar
$(document).ready(function() {
    $('#formInspeccionEsperaModificar').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// inspecciones
// registrar nuevo Inpeccion
$(document).ready(function() {
    $('#formInspeccionEsperaRegistrar').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});
// inspecciones
//empezar inspeccions
$(document).ready(function() {
    $('#formEmpezarInspeccion').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// inspecciones realizados
//modificar inspeccion realizado
$(document).ready(function() {
    $('#formInspeccionModificarRealizado').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// proyecto ejecutar proyecto
$(document).ready(function() {
    $('#formEjecutarProy').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});


// proyecto almacen en espera registro
$(document).ready(function() {
    $('#formproyecto').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// proyecto almacen en espera modificar
$(document).ready(function() {
    $('#formproyectoModificar').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});
// // proyecto obras ejecutadas modificar
$(document).ready(function() {
    $('#formObrasEjecutadas').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// // proyecto luminarias retiradas registro
$(document).ready(function() {
    $('#formregistroLuminariasre').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// // proyecto luminarias retiradas modificar
$(document).ready(function() {
    $('#formRetiradasModificar').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// usuarios  registro
$(document).ready(function() {
    $('#modadRegistraUsuarios_form').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});

// usuarios s modificar
$(document).ready(function() {
    $('#modadModificarUsuarios_form').on('submit', function(e) {
        // Deshabilitar el botón de envío para evitar envíos múltiples
        $(this).find('button[type="submit"]').attr('disabled', true);
        // Puedes mostrar un mensaje de "Enviando..." si lo deseas
        $(this).find('button[type="submit"]').text('Enviando...');
    });
});



